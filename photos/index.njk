---
layout: layouts/base.njk
title: Photos
---

{% for folder in photoNav %}
  {% for photo in folder.photos %}
    <a href="{{ photo.url }}">
      <img src="{{ photo.metadata.jpeg[0].url }}" alt="{{ photo.title }}">
    </a>
  {% endfor %}
{% endfor %}
