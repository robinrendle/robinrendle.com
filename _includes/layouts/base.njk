<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover"> 
    <title>{{ title or metadata.title }}</title>
    <meta
      name="description"
      content="The website of Robin Rendle, a designer and writer from the UK."
    />
    <link rel="apple-touch-icon" sizes="180x180" href="/images/favicons/apple-touch-icon.png">
    <link rel="icon" type="image/png" sizes="32x32" href="/images/favicons/favicon-32x32.png">
    <link rel="icon" type="image/png" sizes="16x16" href="/images/favicons/favicon-16x16.png">
    <link rel="manifest" href="/images/favicons/site.webmanifest">
    <link rel="mask-icon" href="/images/favicons/safari-pinned-tab.svg" color="#ffbf00">
    <meta name="msapplication-TileColor" content="#ffffff">
    <meta name="theme-color" content="#ffffff">
    <link rel="alternate" type="application/rss+xml" title="Notes" href="/feed.xml">
    <link rel="alternate" type="application/rss+xml" title="Newsletter" href="/newsletterfeed.xml">
    <link rel="alternate" type="application/rss+xml" title="Essays" href="/essayfeed.xml">
    <style>


      @font-face {
        font-family: "Hex Franklin";
        src: url('/fonts/hex-franklin/hex-franklin-0.2.woff2') format("woff2");
        font-weight: 1 999;
        font-stretch: 0% 100%;
        font-style: normal;
      }

      @font-face {
        font-family: "Fakir";
        src: url('/fonts/fakir/FakirPro-BlackItalic.woff2') format("woff2");
        font-weight: 600;
        font-style: normal;
      }

      :root {
        --color-web-wrapper: #FFC227;
        --color-text: #111; 
        --color-theme: #FFC227;
        --color-heading: #111;
        --color-gray: #888;
        --color-black: #111;
        --color-white: #eee;
        --color-border: #ccc;
        --color-link-border: #bbb;
        --color-link: #FFC227;
        --color-bg: #fff;
        --space-5: 96px;
        --space-4: 48px;
        --space-34: 36px;
        --space-3: 24px;
        --space-2: 16px;
        --space-1: 8px;
        --grid-space: 15px;
        --grid-article-columns: 1/13;
        --banner-height: 200px;
        --border-width: 2px;
        --border: var(--border-width) solid var(--color-black);
        --border-radius: 2px;
        --content-max-width: 40rem;
        --font-size-1: clamp(10vw, 40vw, 100vw);
        --font-size-3: clamp(1rem, 3vw, 1.15rem);
        --font-size-8: 0.8rem;
        --font-size-small: 1rem;
        --monospace: 'SF Mono', SFMono-Regular, ui-monospace, 'DejaVu Sans Mono', Menlo, Consolas, monospace;
        --sans: "Hex Franklin", Helvetica, -apple-system, BlinkMacSystemFont, avenir next, avenir, segoe ui, helvetica neue, helvetica, Cantarell, Ubuntu, roboto, noto, arial, sans-serif;
        --title: "Fakir", Helvetica, -apple-system, BlinkMacSystemFont, avenir next, avenir, segoe ui, helvetica neue, helvetica, Cantarell, Ubuntu, roboto, noto, arial, sans-serif;
        {# --serif: "GT Alpina", ui-serif, system-ui, serif; #}
        --container-margin: 0 auto;
        --container-padding: 0 8px;
      }

      @media screen and (min-width: 500px) {
        :root {
          --container-padding: 0 20px;
          --banner-height: 200px;
        }
      }

      @media screen and (min-width: 600px) {
        :root {
          --grid-space: 0;
          --grid-article-columns: 2/12;
        }
      }

      @media screen and (min-width: 900px) {
        :root {
          --content-max-width: 54rem;
          --grid-space: 0;
          --banner-height: 350px;
          --grid-article-columns: 3/11;
        }
      }

      img, iframe, video, figure {
        max-width: 100%;
        width: 100%;
      }

      html { 
        min-height: 100%;
        margin: 0;
        padding: 0;
        background: var(--color-link);
        overflow-x: hidden;
        border: var(--border);
      }

      *, *:after, *:before {
        box-sizing: border-box;
      }

      body {
        max-width: 100vw;
        overflow-x: hidden;
        background: var(--color-bg);
        padding: 0;
        margin: 0;
        font: clamp(1.1rem, 3vw, 1.15rem)/1.45 var(--sans);
        color: var(--color-text);
        font-variant-numeric: oldstyle-nums;
        min-height: 100vh;
      }

      @media screen and (min-width: 600px) {
        body {
          font: clamp(1.1rem, 3vw, 1.15rem)/1.55 var(--sans);
        }
      }

      body p {
        hyphens: auto;
        hyphenate-limit-chars: 6 3 3;
        hyphenate-limit-lines: 2;
        hyphenate-limit-last: always;
        hyphenate-limit-zone: 8%;
        margin-top: 0;
        margin-bottom: 1.45rem;
      }

      @media and min-width(800px) {
        body p {
          margin-bottom: 1.65rem;
        }
      }

      body p em {
        font-style: normal;
      }

      body a {
        color: inherit;
        text-decoration-color: var(--color-link-border);
        transition: all .3s ease;
      }

      body a:hover {
        color: var(--color-link);
        text-decoration-color: var(--color-link);
      }

      main {
        min-height: calc(100vh - 40px);
      }

      .main-content {
        display: grid;
        grid-template-columns: repeat(12, 1fr);
        max-width: var(--content-max-width);
        margin: 0 auto;
        gap: 10px;
        padding: 0 var(--grid-space);
        margin-bottom: 2rem;
      }

      .main-content--wide {
        max-width: var(--content-extreme-width);
      }

      {# for images/iframes etc. #}
      .full-width {
        width: 90vw;
        max-width: initial;
        margin-left: 50%;
        transform: translateX(-50%);
        height: auto;
      }

      .content-full {
        grid-column: 1/13;
      }

      .content-list {
        grid-column: var(--grid-article-columns);
        margin-bottom: 70px;
        margin-top: 20px;
      }
      
      .article {
        padding-top: var(--space-3);
        padding-bottom: var(--space-5);
        min-height: 60vh;
      }

      .article img, .article iframe, .article pre[class*="language-"] {
        margin-bottom: var(--space-3);
      }

      @media screen and (min-width: 800px) {
        .article {
          padding-top: var(--space-4);
        }
      }

      .intro-meta-content,
      .article {
        grid-column: var(--grid-article-columns);
      }

      .home-container {
        max-width: 100%;
        padding: 20px 10px 40px 10px;
      }

      .extract-text {
        hyphens: auto;
        text-decoration: none;
        color: var(--color-text);
        hyphens: auto;
        transition: all .3s ease;
        font-size: clamp(1.6rem, 6vw, 2rem);
        line-height: 0.8;
        font-family: var(--title);
        text-transform: uppercase;
      }

      .extract-text:hover {
        filter: url(#noise);
      }

      {# IOS SAFARI HACK FOR STUPID FONT-STRETCH BUG #}
      @media screen and (min-width: 767px) {
        .extract-text {
          font-weight: 300;
          font-stretch: 10%;
          font-size: clamp(2.3rem, 6vw, 3.5rem);
          line-height: 1;
        }

        .home-container {
          max-width: 100%;
          padding: 20px 20px;
        }
      }

      .extract-text:hover,
      .extract-text:focus {
        opacity: 1;
        text-decoration: underline var(--color-heading);
        color: var(--color-heading);
      }

      h1, h2, h3, h4 {
        font-family: var(--sans);
        color: var(--color-heading);
      }

      h2 {
        font-size: clamp(1.3rem, 2vw, 1.5rem);
        line-height: 1.2;
        margin-top: var(--space-4);
      }

      h3 {
        font-size: clamp(1.1rem, 2vw, 1.3rem);
        line-height: 1.2;
        margin-top: var(--space-4);
        color: var(--color-border);
      }

      blockquote {
        margin-left: 0;
        margin-right: 0;
        padding-left: var(--space-2);
        border-left: 2px solid var(--color-border);
      }

      hr {
        border: none;
        display: grid;
        place-items: center;
        font-size: 1rem;
        padding-top: 1rem;
        width: 50px;
        height: 50px;
        text-align: center;
        margin-bottom: 20px;
      }

      hr:after {
        color: var(--color-border);
        width: 50px;
        height: 40px;
        display: block;
        content: "/ / /"; 
      }

      pre code {
        white-space: pre-wrap;
      }

      .inputs {
        display: flex;
        gap: 10px;
        height: 40px;
      }

      .inputs input {
        --webkit-appearance: none;
        display: flex;
        margin: auto;
        height: 100%;
        color: var(--color-gray);
      }

      .inputs input[type="submit"] {
        padding: 0 20px;
        color: var(--color-text);
        background: white;
        border: none;
        border-radius: 3px;
        font-weight: 400;
      }

      input {
        font-family: "Hex Franklin";
      }

      input[type="email"] {
        width: 100%;
        padding-left: 15px;
        background: transparent;
        border: 1px solid #2E2E2E;
        border-radius: var(--border-radius);
      }

      .notes-list {
        list-style: none;
        padding: 0;
      }

      .notelist-item {
        display: flex;
        flex-direction: column;
        margin-bottom: var(--space-3);
        border-bottom: 1px solid var(--color-border);
        padding-bottom: var(--space-3);
      }

      .notelist-item div {
        width: 100%;
      }

      @media screen and (min-width: 700px) {
        .notelist-item {
          flex-direction: row-reverse;
        }
      }

      .notelist-item p {
        margin: 0;
        font-size: 90%;
      }

      .notelist-date {
        font-size: 80%;
        color: var(--color-gray);
        flex-shrink: 0;
      }

      @media screen and (min-width: 700px) {
        .notelist-date {
          margin-right: var(--space-3);
          width: 120px;
          position: relative;
          padding-top: 3px;
        }
      }

      figure {
        margin: 0;
      }
    
      .nav-wrapper {
        background: var(--color-bg);
        color: var(--color-text);
        border-bottom: var(--border-width) solid var(--color-black);
      }

      nav {
        width: min(95%, 48ch);
        display: flex;
        text-align: center;
        font-variant-caps: small-caps;
        text-transform: lowercase;
        font-weight: 400;
        align-items: center;
        font-family: var(--sans);
        font-weight: 500;
      }

      nav a {
        font-size: 17px;
        text-decoration: none;
        color: inherit;
        font-weight: 600;
        padding: 10px 30px 12px 30px;
        border-right: var(--border-width) solid var(--color-black);
        transition: all 0.1s linear;
      }

      @media screen and (min-width: 800) {
        nav a {
          font-size: 17px;
          padding: 14px 30px 16px 30px;
        }
      }

      nav a:hover {
        background: var(--color-black);
      }

      nav a:first-of-type {
        padding-right: 30px;
        padding-left: 30px;
        background: var(--color-black);
        color: var(--color-theme);
      }

      @media screen and (min-width:767px) {
        nav a {
          font-size: 19px;
        }
      }

      .footer-links {
        margin-top: 150px;
        padding: 20px 10px;
        background: black;
        color: white;
        width: 100%;
        font-family: var(--sans);
        font-weight: 600;
        filter: url(#noise);
      }

      a.nav-home {
        color: var(--color-link);
        font-size: 2rem;
      }

      .home-header h1 {
        margin: 0;
      }

      .home-header h2 {
        margin-top: 0;
        font-weight: 400;
      }

      summary:hover {
        cursor: pointer;
      }

      .direct-link {
        display: none;
      }

      pre {
        border-radius: 3px;
      }

      @media screen and (width: 700px) {
        .cp_embed_wrapper {
          width: 80vw;
          margin-left: 20%;
          transform: translateX(-30%);
        }
      }

      .note-header {
        margin-bottom: var(--space-3);
      }
      
      .intro-meta, figcaption {
        font-size: var(--font-size-8);
        color: var(--color-gray);
        font-family: var(--sans);
      }

      .postlist-link {
        font-family: var(--sans);
        font-weight: 600;
        font-size: clamp(1.2rem, 2vw, 1.3rem);
      }

      .notelist-date {
        font-family: var(--sans);
      }

      .essays {
        list-style: none;
        padding: 0;
        display: grid;
        gap: var(--space-2);
        grid-template-columns: 1fr;
        position: relative;
        height: auto;
      }

      @media screen and (min-width: 600px){
        .essays {
          grid-template-columns: 1fr 1fr;
          left: 50%;
          margin-left: -45vw;
          max-width: 90vw;
          margin-right: -45vw;
          position: relative;
          right: 50%;
          height: auto;
        }
      }

      @media screen and (min-width: 1000px){
        .essays {
          grid-template-columns: 1fr 1fr 1fr;
        }
      }

      .essays-item {
        position: relative;
        border: 2px solid black;
        transition: all .2s ease-in;
        border-radius: 3px;
      }

      @media screen and (min-width: 900px){
        .essays-item {
          min-height: 550px;
        }
      }

      .essays-item a {
        display: block;
        height: 100%;
      }

      .essays-item:hover {
        transform: scale(1.03);
      }

      .essays-item:hover:after {
        content: "";
        cursor: pointer;
        pointer-events: none;
        left: 0;
        top: 0;
        right: 0;
        bottom: 0;
        position: absolute;
        width: 100%;
        height: 100%;
        opacity: 0.2;
        z-index: 3;
      }

      .essays-item:hover .essays-item-content {
        background: #000;
        color: white;
      }

      .essays-item-img {
        object-fit: cover;
        height: 100%;
        width: 100%;
      }

      .essays-item-title {
        margin-bottom: 0;
        margin-top: 0;
        font-size: 18px;
        color: white;
        font-weight: 500;
      }

      .essays-item-content {
        position: absolute;
        width: 100%;
        bottom: 0;
        background: black;
        color: var(--color-gray);
        padding: var(--space-3) var(--space-2);
        z-index: 4;
        font-size: 1rem;
      }

      .essays .extract {
        margin-top: 0;
        margin-bottom: 0;
        font-family: var(--sans);
        font-size: inherit;
      }

      abbr {
        font-variant: small-caps;
      }
      
      .pals ul {
        list-style: none;
      }

      .home-link {
        padding-right: 5px;
        padding-left: 5px;
        margin-right: -5px;
      }

      .cell-t40 {
        margin-top: 40px;
      }

      .cell-t80 {
        margin-top: 80px;
      }
    </style>
    {# Code Styles #}
    <style>
      /*
      Name: Duotone Dark
      Author: Simurai, adapted from DuoTone themes for Atom (http://simurai.com/projects/2016/01/01/duotone-themes)
      Conversion: Bram de Haan (http://atelierbram.github.io/Base2Tone-prism/output/prism/prism-base2tone-evening-dark.css)
      Generated with Base16 Builder (https://github.com/base16-builder/base16-builder)
      */

      code[class*="language-"],
      pre[class*="language-"] {
        font-family: var(--monospace);
        font-size: clamp(0.85rem, 2vw, 0.95rem);
        line-height: 1.375;
        direction: ltr;
        text-align: left;
        white-space: pre;
        word-spacing: normal;
        word-break: normal;

        -moz-tab-size: 4;
        -o-tab-size: 4;
        tab-size: 4;

        -webkit-hyphens: none;
        -moz-hyphens: none;
        -ms-hyphens: none;
        hyphens: none;
        background: #2a2734;
        color: #9a86fd;
      }

      pre > code[class*="language-"] {
        font-size: 1em;
      }

      pre[class*="language-"]::-moz-selection, pre[class*="language-"] ::-moz-selection,
      code[class*="language-"]::-moz-selection, code[class*="language-"] ::-moz-selection {
        text-shadow: none;
        background: #6a51e6;
      }

      pre[class*="language-"]::selection, pre[class*="language-"] ::selection,
      code[class*="language-"]::selection, code[class*="language-"] ::selection {
        text-shadow: none;
        background: #6a51e6;
      }

      /* Code blocks */
      pre[class*="language-"] {
        padding: 1em;
        margin: .5em 0;
        overflow: auto;
      }

      /* Inline code */
      :not(pre) > code[class*="language-"] {
        padding: .1em;
        border-radius: .3em;
      }

      .token.comment,
      .token.prolog,
      .token.doctype,
      .token.cdata {
        color: #6c6783;
      }

      .token.punctuation {
        color: #6c6783;
      }

      .token.namespace {
        opacity: .7;
      }

      .token.tag,
      .token.operator,
      .token.number {
        color: #e09142;
      }

      .token.property,
      .token.function {
        color: #9a86fd;
      }

      .token.tag-id,
      .token.selector,
      .token.atrule-id {
        color: #eeebff;
      }

      code.language-javascript,
      .token.attr-name {
        color: #c4b9fe;
      }

      code.language-css,
      code.language-scss,
      .token.boolean,
      .token.string,
      .token.entity,
      .token.url,
      .language-css .token.string,
      .language-scss .token.string,
      .style .token.string,
      .token.attr-value,
      .token.keyword,
      .token.control,
      .token.directive,
      .token.unit,
      .token.statement,
      .token.regex,
      .token.atrule {
        color: #ffcc99;
      }

      .token.placeholder,
      .token.variable {
        color: #ffcc99;
      }

      .token.deleted {
        text-decoration: line-through;
      }

      .token.inserted {
        border-bottom: 1px dotted #eeebff;
        text-decoration: none;
      }

      .token.italic {
        font-style: italic;
      }

      .token.important,
      .token.bold {
        font-weight: bold;
      }

      .token.important {
        color: #c4b9fe;
      }

      .token.entity {
        cursor: help;
      }

      pre > code.highlight {
        outline: .4em solid #8a75f5;
        outline-offset: .4em;
      }

      /* overrides color-values for the Line Numbers plugin
      * http://prismjs.com/plugins/line-numbers/
      */
      .line-numbers.line-numbers .line-numbers-rows {
        border-right-color: #2c2937;
      }

      .line-numbers .line-numbers-rows > span:before {
        color: #3c3949;
      }

      /* overrides color-values for the Line Highlight plugin
      * http://prismjs.com/plugins/line-highlight/
      */
      .line-highlight.line-highlight {
        background: rgba(224, 145, 66, 0.2);
        background: -webkit-linear-gradient(left, rgba(224, 145, 66, 0.2) 70%, rgba(224, 145, 66, 0));
        background: linear-gradient(to right, rgba(224, 145, 66, 0.2) 70%, rgba(224, 145, 66, 0));
      }

      {# Code generated from markdown back ticks: `<html>` #}
      code {
        font-family: var(--monospace);
        font-size: 16px;
        background: var(--color-cream);
        padding: 2px 3px;
        border-radius: 3px;
      }
    </style>
    <style>
      .hover-svg {
        pointer-events: none;
        position: absolute;
      }

      .tmpl-post h2 {
        margin-top: var(--space-4);
      }

      .intro-wrapper {
        background: var(--color-theme);
        container-type: inline-size;
        border-bottom: var(--border);
      }

      .intro-wrapper h1 {
        margin: 0;
        text-align: center;
        text-transform: uppercase;
        font-family: var(--title);
        line-height: 0.8;
        font-size: 15cqw;
        padding-top: 0;
        padding-bottom: 3cqw;
        padding-left: 20px;
        padding-right: 20px;
        pointer-events: none;
        display: grid;
        place-items: center;
        font-variant-numeric: tabular-nums; 
        text-wrap: balance;
        min-height: var(--banner-height);
      }

      .intro-wrapper h1:has(+ .intro-meta) {
        border-bottom: var(--border);
      }

      .intro-wrapper h1.title-short {
        font-size: 28cqw;
        padding-bottom: 4cqw;
      }

      .intro-wrapper h1.title-medium {
        font-size: 15cqw;
        padding-bottom: 0;
      }

      .intro-wrapper h1.title-long {
        font-size: 15cqw;
        padding-top: 6cqw;
        padding-bottom: 6cqw;
      }

      @media screen and (min-width: 800px) {
        .intro-wrapper h1.title-long {
          font-size: 12cqw;
          padding-top: 8cqw;
          padding-bottom: 8cqw;
        } 
      }

      .intro-wrapper h1.title-quite-long {
        font-size: 17cqw;
        padding-top: 8cqw;
        padding-bottom: 10cqw;
      }

      .intro-meta {
        color: var(--color-text);
        display: block;
        padding: var(--space-2) var(--grid-space); 
        display: grid;
        gap: 20px;
        grid-template-columns: repeat(12, 1fr);
        max-width: var(--content-max-width);
        margin: 0 auto;
      }

      .intro-meta-content {
        {# max-width: var(--content-max-width); #}
      }

      .project-item {
        display: flex;
        flex-direction: column;
        margin-bottom: var(--space-3);
        border-bottom: 1px solid var(--color-border);
        padding-bottom: var(--space-3);
      }

      .footer {
        border-top: var(--border);
        background: var(--color-black);
      }

      .footer-title {
        color: var(--color-white);
      }
    </style>
  </head>
  <body>
    <svg class="hover-svg">
      <defs>
        <filter id='noise' x='0%' y='0%' width='150%' height='150%'>
          <feTurbulence baseFrequency="0.01 0.01" numOctaves="1" result="noise"  />
			    <feDisplacementMap in="SourceGraphic" in2="noise" scale="10" xChannelSelector="R" yChannelSelector="R">
        </filter>
      </defs>
    </svg>

    <div class="nav-wrapper">
      <nav>
        <a href="/" class="home-link"> / </a>
        <a href="/about">About</a>
        <a href="/notes">Notes</a>
        <a href="/essays">Essays</a>
        <a href="/newsletter">Newsletter</a>
        {# <a href="/projects">Projects</a> #}
      </nav>
    </div>
    <main{% if templateClass %} class="{{ templateClass }}"{% endif %}>
      {{ content | safe }}
    </main>
    {% include "footer.njk" %}

    <script>
      const title = document.querySelectorAll('#heading')[0];
      const titleLetters = title.textContent.length;
      const titleWords = title.textContent.trim().split(/\s+/).length;

      if(titleLetters > 15) {
        title.classList.add('title-long');
      } 

      if(titleLetters > 10) {
        title.classList.add('title-medium');
      }

      if(titleLetters < 10) {
        title.classList.add('title-short');
      }

      if(titleLetters < 10 && titleWords > 1) {
        title.classList.remove('title-short');
        title.classList.add('title-medium');
      }

      if(titleLetters > 6 && titleWords < 2) {
        title.classList.remove('title-short');
        title.classList.remove('title-long');
        title.classList.add('title-medium');
      }

      if(titleLetters > 8 && titleWords < 2) {
        title.classList.remove('title-short');
        title.classList.remove('title-medium');
        title.classList.add('title-quite-long');
      }

      if(titleWords > 7) {
        title.classList.remove('title-medium');
        title.classList.add('title-long');
      }
    </script>
  </body>
</html>