<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title or metadata.title }}</title>
    <meta
      name="description"
      content="The website of Robin Rendle, a designer and writer from the UK."
    />
    <link rel="apple-touch-icon" sizes="180x180" href="/images/favicons/apple-touch-icon.png">
    <link rel="icon" type="image/png" sizes="32x32" href="/images/favicons/favicon-32x32.png">
    <link rel="icon" type="image/png" sizes="16x16" href="/images/favicons/favicon-16x16.png">
    <link rel="manifest" href="/images/favicons/site.webmanifest">
    <link rel="mask-icon" href="/images/favicons/safari-pinned-tab.svg" color="#ffbf00">
    <meta name="msapplication-TileColor" content="#ffffff">
    <meta name="theme-color" content="#ffffff">
    <link rel="alternate" type="application/rss+xml" title="Notes RSS feed" href="/feed.xml">
    <style>
      @font-face {
        font-family: "GT Alpina";
        src: url("/fonts/alpina/GT-Alpina-Standard-Regular.woff2") format("woff2");
      }

      @font-face {
        font-family: "GT Alpina";
        src: url("/fonts/alpina/GT-Alpina-Standard-Regular-Italic.woff2") format("woff2");
        font-style: italic;
      }

      @font-face {
        font-family: "GT Alpina";
        font-weight: 600;
        src: url("/fonts/alpina/GT-Alpina-Standard-Bold.woff2") format("woff2");
      }

      @font-face {
        font-family: "Hex Franklin";
        src: url('/fonts/hex-franklin/hex-franklin-0.2.woff2') format("woff2");
        font-weight: 1 999;
        font-stretch: 0% 100%;
        font-style: normal;
      }

      :root {
        --color-web-wrapper: #FFC227;
        --color-text: #333; 
        --color-heading: #111;
        --color-gray: #888;
        --color-border: #ccc;
        --color-link-border: #bbb;
        --color-link: #FFC227;
        --color-bg: #fff;
        --space-5: 96px;
        --space-4: 48px;
        --space-34: 36px;
        --space-3: 24px;
        --space-2: 16px;
        --space-1: 8px;
        --font-size-1: clamp(1.6rem, 3vw, 1.8rem);
        --font-size-3: clamp(1rem, 3vw, 1.15rem);
        --font-size-8: 0.8rem;
        --font-size-small: 1rem;
        --monospace: 'SF Mono', SFMono-Regular, ui-monospace, 'DejaVu Sans Mono', Menlo, Consolas, monospace;
        --sans: "Hex Franklin", Helvetica, -apple-system, BlinkMacSystemFont, avenir next, avenir, segoe ui, helvetica neue, helvetica, Cantarell, Ubuntu, roboto, noto, arial, sans-serif;
        --serif: "GT Alpina", ui-serif, system-ui, serif;
        --container-margin: 0 auto;
        --container-padding: 0 8px;
      }

      @media screen and (min-width: 500px) {
        :root {
          --container-padding: 0 20px;
        }
      }

      img, iframe, video, figure {
        max-width: 100%;
      }

      html { 
        min-height: 100%;
        margin: 0;
        padding: 0;
        border-top: 2px solid var(--color-web-wrapper);
        border-bottom: 0px solid var(--color-web-wrapper);
        background: var(--color-link);
        overflow-x: hidden;
      }

      @media screen and (min-width: 600px) {
        html {
          border-top: 5px solid var(--color-web-wrapper);
          border-bottom: 5px solid var(--color-web-wrapper);
          border-left: 5px solid var(--color-web-wrapper);
          border-right: 5px solid var(--color-web-wrapper);
        }
      }

      *, *:after, *:before {
        box-sizing: border-box;
      }

      body {
        max-width: 100vw;
        overflow-x: hidden;
        background: var(--color-bg);
        padding: 0 0 var(--space-5) 0;
        margin: 0;
        font: clamp(1.2rem, 3vw, 1.3rem)/1.35 var(--serif);
        color: var(--color-text);
        font-variant-numeric: oldstyle-nums;
        min-height: 100vh;
      }

      @media screen and (min-width: 600px) {
        body {
          font: clamp(1.2rem, 3vw, 1.3rem)/1.4 var(--serif);
        }
      }

      body p {
        hyphens: auto;
        hyphenate-limit-chars: 6 3 3;
        hyphenate-limit-lines: 2;
        hyphenate-limit-last: always;
        hyphenate-limit-zone: 8%;
      }

      body a {
        color: inherit;
        text-decoration-color: var(--color-link-border);
        transition: all .3s ease;
      }

      body a:hover {
        color: var(--color-link);
        text-decoration-color: var(--color-link);
      }

      main {
        width: min(95%, 52ch);
        margin: var(--container-margin);
        margin-top: var(--space-34);
        padding: var(--container-padding);
      }

      @media screen and (min-width: 767px) {
        main {
          margin-top: var(--space-4);
        }
      }

      main.full {
        width: 100%;
        margin: 0;
        padding: 20px 20px;
      }

      .home-container {
        max-width: 100%;
      }

      .extract-text {
        hyphens: auto;
        text-decoration: none;
        color: var(--color-heading);
        transition: all .3s ease;
        font-family: var(--sans);
        font-size: 1.1em;
      }

      {# .extract-text:after {
        content: "/";
        opacity: 0.2;
        padding-right: 10px;
      } #}

      {# IOS SAFARI HACK FOR STUPID FONT-STRETCH BUG #}
      @media screen and (min-width: 767px) {
        .extract-text {
          font-weight: 300;
          font-stretch: 10%;
          font-size: clamp(2.3rem, 6vw, 3.5rem);
          line-height: 1.2;
        }
      }

      .extract-text:hover,
      .extract-text:focus {
        font-weight: 999;
        text-decoration: underline var(--color-link);
        color: var(--color-heading);
      }

      h1 {
        font-size: var(--font-size-1);
        line-height: 1.2;
        font-feature-settings: "ss06";
        font-weight: 650;
      }

      h1, h2, h3, h4 {
        font-family: var(--sans);
        color: var(--color-heading);
      }

      h2 {
        font-size: clamp(1.3rem, 2vw, 1.5rem);
        line-height: 1.2;
        margin-top: var(--space-4);
      }

      h3 {
        font-size: clamp(1.1rem, 2vw, 1.3rem);
        line-height: 1.2;
        margin-top: var(--space-4);
        color: var(--color-border);
      }

      blockquote {
        margin-left: 0;
        margin-right: 0;
        padding-left: var(--space-2);
        border-left: 2px solid var(--color-border);
      }

      hr {
        border: none;
        border-top: 1px solid var(--color-web-wrapper);
      }

      pre code {
        white-space: pre-wrap;
      }

      .inputs {
        display: flex;
        gap: 10px;
        height: 40px;
      }

      .inputs input {
        display: flex;
        margin: auto;
        height: 100%;
      }

      input {
        font-family: "Hex Franklin";
      }

      input[type="email"] {
        width: 100%;
        padding-left: 15px;
      }

      .notes-list {
        list-style: none;
        padding: 0;
      }

      .notelist-item {
        display: flex;
        flex-direction: column;
        margin-bottom: var(--space-3);
        border-bottom: 1px solid var(--color-border);
        padding-bottom: var(--space-3);
      }

      .notelist-item div {
        width: 100%;
      }

      @media screen and (min-width: 700px) {
        .notelist-item {
          flex-direction: row-reverse;
        }
      }

      .notelist-item p {
        margin: 0;
      }

      .notelist-date {
        font-size: 80%;
        color: var(--color-gray);
        flex-shrink: 0;
      }

      @media screen and (min-width: 700px) {
        .notelist-date {
          margin-right: var(--space-3);
          width: 120px;
          position: relative;
          padding-top: 3px;
        }
      }

      figure {
        margin: 0;
      }
    
      .nav-wrapper {
        background: var(--color-heading);
        color: white;
      }

      nav {
        width: min(95%, 48ch);
        padding: 10px 0;
        display: flex;
        gap: var(--space-2);
        text-align: center;
        font-variant-caps: small-caps;
        text-transform: lowercase;
        font-weight: 400;
        align-items: center;
        margin: var(--container-margin);
        padding: var(--container-padding);
        padding-top: 20px;
        padding-bottom: 20px;
        font-family: var(--sans);
        font-weight: 500;
      }

      nav a {
        font-size: 17px;
        text-decoration: none;
        color: inherit;
      }

      @media screen and (min-width:767px) {
        nav a {
          font-size: 18px;
        }
      }

      .footer-links {
        margin-top: 150px;
        padding: 20px 10px;
        background: black;
        color: white;
        width: 100%;
        font-family: var(--sans);
        font-weight: 600;
        filter: url(#noise);
      }

      a.nav-home {
        color: var(--color-link);
        font-size: 2rem;
      }

      .home-header h1 {
        margin: 0;
      }

      .home-header h2 {
        margin-top: 0;
        font-weight: 400;
      }

      summary:hover {
        cursor: pointer;
      }

      .direct-link {
        display: none;
      }

      pre {
        border-radius: 3px;
      }

      @media screen and (width: 700px) {
        .cp_embed_wrapper {
          width: 80vw;
          margin-left: 20%;
          transform: translateX(-30%);
        }
      }
    </style>
    {# Note styles #}
    <style>
      .note-header {
        margin-bottom: var(--space-3);
      }
      
      .note-meta, figcaption {
        font-size: var(--font-size-8);
        color: var(--color-gray);
        font-family: var(--sans);
      }

      .note-title {
        margin-bottom: 5px;
      }

      .postlist-link {
        font-family: var(--sans);
        font-weight: 600;
        font-size: clamp(1.2rem, 2vw, 1.3rem);
      }

      .notelist-date {
        font-family: var(--sans);
      }
    </style>
    <style>
      .essays {
        list-style: none;
        padding: 0;
        display: grid;
        gap: var(--space-2);
        grid-template-columns: 1fr;
        position: relative;
        height: auto;
        margin-top: -20px;
      }

      @media screen and (min-width: 600px){
        .essays {
          grid-template-columns: 1fr 1fr;
          left: 50%;
          margin-left: -45vw;
          max-width: 90vw;
          margin-right: -45vw;
          position: relative;
          right: 50%;
          height: auto;
        }
      }

      @media screen and (min-width: 1000px){
        .essays {
          grid-template-columns: 1fr 1fr 1fr;
        }
      }

      .essays-item {
        position: relative;
        border: 2px solid black;
        transition: all .2s ease-in;
        border-radius: 3px;
      }

      @media screen and (min-width: 900px){
        .essays-item {
          min-height: 550px;
        }
      }

      .essays-item a {
        display: block;
        height: 100%;
      }

      .essays-item:hover {
        transform: scale(1.03);
      }

      .essays-item:hover:after {
        content: "";
        cursor: pointer;
        pointer-events: none;
        left: 0;
        top: 0;
        right: 0;
        bottom: 0;
        position: absolute;
        width: 100%;
        height: 100%;
        opacity: 0.2;
        z-index: 3;
      }

      .essays-item:hover .essays-item-content {
        background: #000;
        color: white;
      }

      .essays-item-img {
        object-fit: cover;
        height: 100%;
        width: 100%;
      }

      .essays-item-title {
        margin-bottom: 0;
        margin-top: 0;
        font-size: 18px;
        color: white;
        font-weight: 500;
      }

      .essays-item-content {
        position: absolute;
        width: 100%;
        bottom: 0;
        background: black;
        color: var(--color-gray);
        padding: var(--space-3) var(--space-2);
        z-index: 4;
        font-size: 1rem;
      }

      .essays .extract {
        margin-top: 0;
        margin-bottom: 0;
        font-family: var(--sans);
        font-size: inherit;
      }

      abbr {
        font-variant: small-caps;
      }

      .title {
        display: block;
        left: 50%;
        margin-top: -50px;
        margin-left: -50vw;
        margin-bottom: 10px;
        max-width: 100vw;
        margin-right: -50vw;
        position: relative;
        right: 50%;
        width: 100vw;
        height: auto;
        font-stretch: 10%;
        font-size: 43vw;
        line-height: 0.8;
        z-index: 999;
        text-transform: uppercase;
        text-align: center;
        letter-spacing: -5px;
        pointer-events: none;
        margin-bottom: 100px;
        margin-top: 10px;
      }

      @media screen and (min-width: 600px) {
        .title {
          margin-left: calc(-50vw + 5px);
          max-width: calc(100vw - 10px);
          margin-bottom: 100px;
        }
      }
    
      .newsletter {
        --color-link-border: #888;
        background: #111;
        color: white;
        padding: 30px;
        border-radius: 4px;
        transform: rotate(0.4deg);
        margin-bottom: 40px;
      }

      .newsletter:nth-child(even) {
        transform: rotate(-0.4deg);
      }

      .newsletter input[type="email"] {
        border: none;
        border-bottom: 1px solid #444;
        background: transparent;
        padding-left: 0;
      }

      .newsletter input[type="submit"] {
        background: white;
        border: none;
        border-radius: 4px;
        color: black;
      }

      .newsletter input[type="submit"]:hover {
        filter: url(#noise);
        cursor: pointer;
      }

      .newsletter input[type="email"]:focus {
        border-bottom: 3px solid #111;
        padding-left: 5px;
        transition: all .3s ease;
      }

      .newsletter p {
        margin-top: 0;
      }

      .pals ul {
        list-style: none;
      }

      .home-link {
        padding-right: 5px;
        padding-left: 5px;
        margin-right: -5px;
      }
</style>
    </style>


    {# Helpers #}
    <style>
      .cell-t80 {
        margin-top: 80px;
      }
    </style>

    {# Code Styles #}
    <style>
      /*
      Name: Duotone Dark
      Author: Simurai, adapted from DuoTone themes for Atom (http://simurai.com/projects/2016/01/01/duotone-themes)
      Conversion: Bram de Haan (http://atelierbram.github.io/Base2Tone-prism/output/prism/prism-base2tone-evening-dark.css)
      Generated with Base16 Builder (https://github.com/base16-builder/base16-builder)
      */

      code[class*="language-"],
      pre[class*="language-"] {
        font-family: var(--monospace);
        font-size: clamp(0.85rem, 2vw, 0.95rem);
        line-height: 1.375;
        direction: ltr;
        text-align: left;
        white-space: pre;
        word-spacing: normal;
        word-break: normal;

        -moz-tab-size: 4;
        -o-tab-size: 4;
        tab-size: 4;

        -webkit-hyphens: none;
        -moz-hyphens: none;
        -ms-hyphens: none;
        hyphens: none;
        background: #2a2734;
        color: #9a86fd;
      }

      pre > code[class*="language-"] {
        font-size: 1em;
      }

      pre[class*="language-"]::-moz-selection, pre[class*="language-"] ::-moz-selection,
      code[class*="language-"]::-moz-selection, code[class*="language-"] ::-moz-selection {
        text-shadow: none;
        background: #6a51e6;
      }

      pre[class*="language-"]::selection, pre[class*="language-"] ::selection,
      code[class*="language-"]::selection, code[class*="language-"] ::selection {
        text-shadow: none;
        background: #6a51e6;
      }

      /* Code blocks */
      pre[class*="language-"] {
        padding: 1em;
        margin: .5em 0;
        overflow: auto;
      }

      /* Inline code */
      :not(pre) > code[class*="language-"] {
        padding: .1em;
        border-radius: .3em;
      }

      .token.comment,
      .token.prolog,
      .token.doctype,
      .token.cdata {
        color: #6c6783;
      }

      .token.punctuation {
        color: #6c6783;
      }

      .token.namespace {
        opacity: .7;
      }

      .token.tag,
      .token.operator,
      .token.number {
        color: #e09142;
      }

      .token.property,
      .token.function {
        color: #9a86fd;
      }

      .token.tag-id,
      .token.selector,
      .token.atrule-id {
        color: #eeebff;
      }

      code.language-javascript,
      .token.attr-name {
        color: #c4b9fe;
      }

      code.language-css,
      code.language-scss,
      .token.boolean,
      .token.string,
      .token.entity,
      .token.url,
      .language-css .token.string,
      .language-scss .token.string,
      .style .token.string,
      .token.attr-value,
      .token.keyword,
      .token.control,
      .token.directive,
      .token.unit,
      .token.statement,
      .token.regex,
      .token.atrule {
        color: #ffcc99;
      }

      .token.placeholder,
      .token.variable {
        color: #ffcc99;
      }

      .token.deleted {
        text-decoration: line-through;
      }

      .token.inserted {
        border-bottom: 1px dotted #eeebff;
        text-decoration: none;
      }

      .token.italic {
        font-style: italic;
      }

      .token.important,
      .token.bold {
        font-weight: bold;
      }

      .token.important {
        color: #c4b9fe;
      }

      .token.entity {
        cursor: help;
      }

      pre > code.highlight {
        outline: .4em solid #8a75f5;
        outline-offset: .4em;
      }

      /* overrides color-values for the Line Numbers plugin
      * http://prismjs.com/plugins/line-numbers/
      */
      .line-numbers.line-numbers .line-numbers-rows {
        border-right-color: #2c2937;
      }

      .line-numbers .line-numbers-rows > span:before {
        color: #3c3949;
      }

      /* overrides color-values for the Line Highlight plugin
      * http://prismjs.com/plugins/line-highlight/
      */
      .line-highlight.line-highlight {
        background: rgba(224, 145, 66, 0.2);
        background: -webkit-linear-gradient(left, rgba(224, 145, 66, 0.2) 70%, rgba(224, 145, 66, 0));
        background: linear-gradient(to right, rgba(224, 145, 66, 0.2) 70%, rgba(224, 145, 66, 0));
      }

      {# Code generated from markdown back ticks: `<html>` #}
      code {
        font-family: var(--monospace);
        font-size: 16px;
        background: var(--color-cream);
        padding: 2px 3px;
        border-radius: 3px;
      }
    </style>
    <style>
      .about-img.no-margin {
        margin-top: 0;
      }
      .about-img img {
        min-width: 100%;
      }
      .about-img {
        left: 50%;
        margin-top: -50px;
        margin-left: -50vw;
        max-width: 100vw;
        margin-right: -50vw;
        position: relative;
        right: 50%;
        width: 100vw;
        height: auto;
      }
      @media screen and (min-width: 600px) {
        .about-img {
          margin-left: calc(-50vw + 5px);
          max-width: calc(100vw - 10px);
        }
      }

      .about-img figcaption {
        padding-left: 20px;
        margin-bottom: 40px;
      }

      .title {
	      filter: url(#noise);
      }
      
      a:hover {
        filter: url(#noise);
      }

      .hover-svg {
        pointer-events: none;
        position: absolute;
      }

      .tmpl-post h2 {
        margin-top: var(--space-4);
      }

    </style>
  </head>
  <body>
    <svg class="hover-svg">
      <defs>
        <filter id='noise' x='0%' y='0%' width='150%' height='150%'>
          <feTurbulence baseFrequency="0.01 0.01" numOctaves="1" result="noise"  />
			    <feDisplacementMap in="SourceGraphic" in2="noise" scale="10" xChannelSelector="R" yChannelSelector="R">
        </filter>
      </defs>
    </svg>

    <div class="nav-wrapper">
      <nav>
        <a href="/" class="home-link"> / </a>
        <a href="/about">About</a>
        <a href="/notes">Notes</a>
        <a href="/essays">Essays</a>
        <a href="/work">Work</a>
        <a href="/newsletter">Newsletter</a>
      </nav>
    </div>
    <main{% if templateClass %} class="{{ templateClass }}"{% endif %}>
      {{ content | safe }}
    </main>
  </body>
</html>